// import { MemoryCache } from 'memory-cache-node';

// export default class Caching {
//   private cache: MemoryCache;

//   constructor() {
//     // Use a custom key function that combines latitude and longitude
//     const keyFunction = (coords: { lat: number; long: number }) =>
//       `${coords.lat}_${coords.long}`;

//     this.cache = new MemoryCache<[{ lat: number; long: number }], number>();
//   }
// }
